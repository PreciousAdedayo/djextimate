# Database (SQLite by default - works out of the box)
DATABASE_URL="file:./dev.db"

# Admin login (used by /admin)
ADMIN_EMAIL="admin@extimate.com"
ADMIN_PASSWORD="ChangeMe123!"

# Random secret used to sign admin session cookies.
# Generate one with: openssl rand -base64 32
AUTH_SECRET="replace-this-with-a-long-random-string"
